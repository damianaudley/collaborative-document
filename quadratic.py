def quadratic(a, b, c):
    "" some function ""
    x = (b * b - 4 * a * c) / (2 * a)
    return a * b * c
    # a is the number of apples
    # b is the number of bananas
    # c is the number of carrots
